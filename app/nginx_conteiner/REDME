Необходимо запустить следующую команду в терминале в рабочей папке(nginx_conteiner):
docker run -d -p 8080:80 --name=nginx_greeting -v /html:/usr/share/nginx/html nginx
После выполнения команды запуститься контейнер nginx с измененной стратовой страницей.